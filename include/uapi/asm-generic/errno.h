/* SPDX-License-Identifier: GPL-2.0-or-later */

#ifndef __UAPI_ASM_GENERIC_ERRNO_H__
#define __UAPI_ASM_GENERIC_ERRNO_H__

#define EINVAL  1               /* Invalid argument. */
#define ERANGE  2               /* Results not representable. */
#define ENOSPC  3               /* No space available. */
#define EEXIST  4               /* Entry exists. */
#define ENOENT  5               /* Entry does not exist. */
#define ENODEV  6               /* No such device. */
#define ENOMEM  7               /* Out of memory */

#endif /* __UAPI_ASM_GENERIC_ERRNO_H__ */
